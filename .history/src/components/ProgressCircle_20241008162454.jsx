import { Box, useTheme } from '@mui/material';
import { tokens } from '../theme';

const ProgressCircle = ({ isDashboard = false }) => {
    return (
        <div>ProgressCircle</div>
    )
}

export default ProgressCircle